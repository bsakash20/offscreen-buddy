/**
 * Platform-specific stubs for Android modules
 * These stubs provide safe fallbacks for Android-only modules when running on iOS
 * Returns null on iOS since Android-specific functionality is not available
 */
export default null;